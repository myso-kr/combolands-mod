# Combolands — mod

Korean language patch, cheat widget, and autoplay for the Steam roguelike citybuilder
*Combolands: Roguelike Citybuilder* (Crux Games, AppID 4075620).

> **Status: language patch and cheat widget work.** Every string the game has is
> translated and renders in the retail build, and the cheat panel is on F8.
> Autoplay is not written yet. No game file is modified — the loader and the mod are
> added beside them and uninstall by deletion.

| Document | What it settles |
|---|---|
| [docs/PLAN.md](docs/PLAN.md) | what this is, what it attaches to, and the order the work happens in |
| [docs/CONVENTIONS.md](docs/CONVENTIONS.md) | where files go |
| [docs/ANCHORS.md](docs/ANCHORS.md) | what breaks when the game updates, and where to fix it |

## Translation

<!-- generated by: python tools/status.py --markdown -->

| Language | Names | Descriptions | UI | Total |
|---|---|---|---|---|
| `ko` | 441/441 | 385/385 | 245/245 | 1071/1071 (100.0%) |

1,088 keys come out of the game; 17 of them are nothing but `[tokens]` and read the
same in every language, so they are left alone.

## How it works

The game already had a localisation scaffold — every translatable string is a
`LocalizedStringAsset` with a `Key` — so the whole translation is one Harmony
postfix on `GetText()`, joined on the key rather than on the English. Crux can
reword their own text without moving ours.

None of the game's five TextMeshPro fonts has a single Hangul glyph, so the mod
builds a **Galmuri11** font asset from a TTF at runtime, fills its atlas on demand,
and hangs it off every font as a fallback — re-registering on each scene load,
because two of those fonts only appear with the game scene.

## Cheat widget

**F8.** Gold, score, rerolls, removes, dismisses, rewinds, enchantment; weeks,
score target, milestone, shop; placement restrictions; the game's own scoring-speed
flag; and the two permanent guild unlocks, which ask twice because abandoning the
run will not undo them.

**Using any cheat stops Steam achievements for the rest of the session.** That is
the default, not a setting to find, because an achievement unlocked by a cheat
cannot be taken back. `BlockAchievements = false` turns it off, and the panel always
says which state it is in.

The game also still has Crux's own developer cheats, untouched by this mod: hold
`RightShift`+`C`+`L` to arm them, then `RightShift` plus `G`, `B`, `W`, `E` and the
rest. Those do **not** trip the achievement gate — the mod cannot see them.

## Configuration

`UserData/MelonPreferences.cfg`, written on first run:

| Key | Default | |
|---|---|---|
| `Language` | `ko` | folder under `UserData/Combolands/locale`. Empty turns translation off |
| `Translate` | `true` | |
| `FontScale` | `0.8` | Korean glyph size against the game's own font. Hangul fills its em box and Latin does not, so 1.0 towers |
| `FontSamplingPointSize` | `32` | atlas resolution, not rendered size |
| `FontFile` | `Galmuri11.ttf` | under `UserData/Combolands/fonts` |
| `DumpStrings` | `false` | developer: write the English out to `generated/` |
| `Cheats` | `true` | |
| `WidgetKey` | `F8` | any `UnityEngine.KeyCode` name |
| `WidgetScale` | `0` | panel scale. 0 derives one from the window height — IMGUI has no DPI awareness |
| `BlockAchievements` | `true` | stop submitting achievements once any cheat is used |

## Building

```
dotnet build src/Combolands.Mod -c Release
python tools/lint-locale.py locale/ko/strings.json --english generated/strings.en.json
python tools/status.py
```

`GameDir` defaults to the Steam path and is overridable:
`-p:GameDir="D:\SteamLibrary\steamapps\common\Combolands"`.

---

> **Unofficial fan-made mod.** Not affiliated with or endorsed by Crux Games or
> Valve. Requires a legitimately purchased copy of the game — this repository
> contains no original game text or assets. Cheats and autoplay can corrupt your
> save; back it up first. Provided as is, with no warranty. See [NOTICE](NOTICE).
